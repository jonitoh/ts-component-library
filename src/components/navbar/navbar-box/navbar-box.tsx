import React from 'react';
import styles from './navbar-box.module.scss';

export type Props = Object;

export default function NavbarBox(props: Props) {
  // const dataBox = useQuery(GetMainNavigationQuery);
  return <div>For now this is empty !!!</div>;
}
